/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltroPipe } from './filtro.pipe';

describe('FiltroPipe', () => {
  it('create an instance', () => {
    let pipe = new FiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
